# Lemma-tests for *adjectives* in ...`adjectives.lexc`


**bengwarr** failures:

* `bengwarr+A+Sg+Nom` does not generate!
* `bengwarr` has no analyses either

**ngoreng** failures:

* `ngoreng+A+Sg+Nom` does not generate!
* `ngoreng` has no analyses either

**yarrkka** failures:

* `yarrkka+A+Sg+Nom` does not generate!
* `yarrkka` has no analyses either

**balekeno** failures:

* `balekeno+A+Sg+Nom` does not generate!
* `balekeno` has no analyses either

**kaddambu** failures:

* `kaddambu+A+Sg+Nom` does not generate!
* `kaddambu` has no analyses either

**kilel** failures:

* `kilel+A+Sg+Nom` does not generate!
* `kilel` has no analyses either

**warribo** failures:

* `warribo+A+Sg+Nom` does not generate!
* `warribo` has no analyses either

**banj** failures:

* `banj+A+Sg+Nom` does not generate!
* `banj` has no analyses either

**yerre** failures:

* `yerre+A+Sg+Nom` does not generate!
* `yerre` has no analyses either

**lorrk** failures:

* `lorrk+A+Sg+Nom` does not generate!
* `lorrk` has no analyses either

**ngukmak** failures:

* `ngukmak+A+Sg+Nom` does not generate!
* `ngukmak` has no analyses either

**djan** failures:

* `djan+A+Sg+Nom` does not generate!
* `djan` has no analyses either

**kebbabi** failures:

* `kebbabi+A+Sg+Nom` does not generate!
* `kebbabi` has no analyses either

**darnkih** failures:

* `darnkih+A+Sg+Nom` does not generate!
* `darnkih` has no analyses either

**murrngrayek** failures:

* `murrngrayek+A+Sg+Nom` does not generate!
* `murrngrayek` has no analyses either

**balay** failures:

* `balay+A+Sg+Nom` does not generate!
* `balay` has no analyses either

**mobik** failures:

* `mobik+A+Sg+Nom` does not generate!
* `mobik` has no analyses either

**kelebuk** failures:

* `kelebuk+A+Sg+Nom` does not generate!
* `kelebuk` has no analyses either

**wowarre** failures:

* `wowarre+A+Sg+Nom` does not generate!
* `wowarre` has no analyses either

**duninj** failures:

* `duninj+A+Sg+Nom` does not generate!
* `duninj` has no analyses either

**warnmak** failures:

* `warnmak+A+Sg+Nom` does not generate!
* `warnmak` has no analyses either

**dubbe** failures:

* `dubbe+A+Sg+Nom` does not generate!
* `dubbe` has no analyses either

**mirrk** failures:

* `mirrk+A+Sg+Nom` does not generate!
* `mirrk` has no analyses either

**malaywi** failures:

* `malaywi+A+Sg+Nom` does not generate!
* `malaywi` has no analyses either

**djarrehwurd** failures:

* `djarrehwurd+A+Sg+Nom` does not generate!
* `djarrehwurd` has no analyses either

**djurdudjurdu** failures:

* `djurdudjurdu+A+Sg+Nom` does not generate!
* `djurdudjurdu` has no analyses either

**kebmak** failures:

* `kebmak+A+Sg+Nom` does not generate!
* `kebmak` has no analyses either

**djarrkno** failures:

* `djarrkno+A+Sg+Nom` does not generate!
* `djarrkno` has no analyses either

**kangerdken** failures:

* `kangerdken+A+Sg+Nom` does not generate!
* `kangerdken` has no analyses either

**kerrnge** failures:

* `kerrnge+A+Sg+Nom` does not generate!
* `kerrnge` has no analyses either

**kunbidbokenh** failures:

* `kunbidbokenh+A+Sg+Nom` does not generate!
* `kunbidbokenh` has no analyses either

**djumbung** failures:

* `djumbung+A+Sg+Nom` does not generate!
* `djumbung` has no analyses either

**warre** failures:

* `warre+A+Sg+Nom` does not generate!
* `warre` has no analyses either

**rowk** failures:

* `rowk+A+Sg+Nom` does not generate!
* `rowk` has no analyses either

**kang** failures:

* `kang+A+Sg+Nom` does not generate!
* `kang` has no analyses either

**kaddum** failures:

* `kaddum+A+Sg+Nom` does not generate!
* `kaddum` has no analyses either

**wernkih** failures:

* `wernkih+A+Sg+Nom` does not generate!
* `wernkih` has no analyses either

**wid** failures:

* `wid+A+Sg+Nom` does not generate!
* `wid` has no analyses either

**buyu** failures:

* `buyu+A+Sg+Nom` does not generate!
* `buyu` has no analyses either

**djamun** failures:

* `djamun+A+Sg+Nom` does not generate!
* `djamun` has no analyses either

**ngulken** failures:

* `ngulken+A+Sg+Nom` does not generate!
* `ngulken` has no analyses either

**modmiken** failures:

* `modmiken+A+Sg+Nom` does not generate!
* `modmiken` has no analyses either

**dordidordi** failures:

* `dordidordi+A+Sg+Nom` does not generate!
* `dordidordi` has no analyses either

**kodjkewarre** failures:

* `kodjkewarre+A+Sg+Nom` does not generate!
* `kodjkewarre` has no analyses either

**barli** failures:

* `barli+A+Sg+Nom` does not generate!
* `barli` has no analyses either

**wob** failures:

* `wob+A+Sg+Nom` does not generate!
* `wob` has no analyses either

**kimuk** failures:

* `kimuk+A+Sg+Nom` does not generate!
* `kimuk` has no analyses either

**woybukki** failures:

* `woybukki+A+Sg+Nom` does not generate!
* `woybukki` has no analyses either

**kare** failures:

* `kare+A+Sg+Nom` does not generate!
* `kare` has no analyses either

**bolkkime** failures:

* `bolkkime+A+Sg+Nom` does not generate!
* `bolkkime` has no analyses either

**badjan** failures:

* `badjan+A+Sg+Nom` does not generate!
* `badjan` has no analyses either

**baybay** failures:

* `baybay+A+Sg+Nom` does not generate!
* `baybay` has no analyses either

**darrkid** failures:

* `darrkid+A+Sg+Nom` does not generate!
* `darrkid` has no analyses either

**kedjelk** failures:

* `kedjelk+A+Sg+Nom` does not generate!
* `kedjelk` has no analyses either

**warridj** failures:

* `warridj+A+Sg+Nom` does not generate!
* `warridj` has no analyses either

**yeledj** failures:

* `yeledj+A+Sg+Nom` does not generate!
* `yeledj` has no analyses either

**yirrek** failures:

* `yirrek+A+Sg+Nom` does not generate!
* `yirrek` has no analyses either

**buyika** failures:

* `buyika+A+Sg+Nom` does not generate!
* `buyika` has no analyses either

**kodjbarla** failures:

* `kodjbarla+A+Sg+Nom` does not generate!
* `kodjbarla` has no analyses either

**borledmiken** failures:

* `borledmiken+A+Sg+Nom` does not generate!
* `borledmiken` has no analyses either

**makkayken** failures:

* `makkayken+A+Sg+Nom` does not generate!
* `makkayken` has no analyses either

**kunkare** failures:

* `kunkare+A+Sg+Nom` does not generate!
* `kunkare` has no analyses either

**munun** failures:

* `munun+A+Sg+Nom` does not generate!
* `munun` has no analyses either

**yidmebabang** failures:

* `yidmebabang+A+Sg+Nom` does not generate!
* `yidmebabang` has no analyses either

**burlerri** failures:

* `burlerri+A+Sg+Nom` does not generate!
* `burlerri` has no analyses either

**mak** failures:

* `mak+A+Sg+Nom` does not generate!
* `mak` has no analyses either

**rohrok** failures:

* `rohrok+A+Sg+Nom` does not generate!
* `rohrok` has no analyses either

**korroko** failures:

* `korroko+A+Sg+Nom` does not generate!
* `korroko` has no analyses either

**kerlk** failures:

* `kerlk+A+Sg+Nom` does not generate!
* `kerlk` has no analyses either

**darrinj** failures:

* `darrinj+A+Sg+Nom` does not generate!
* `darrinj` has no analyses either

**kukebrok** failures:

* `kukebrok+A+Sg+Nom` does not generate!
* `kukebrok` has no analyses either

**dulmuk** failures:

* `dulmuk+A+Sg+Nom` does not generate!
* `dulmuk` has no analyses either

**warlah** failures:

* `warlah+A+Sg+Nom` does not generate!
* `warlah` has no analyses either

**munguyh** failures:

* `munguyh+A+Sg+Nom` does not generate!
* `munguyh` has no analyses either

**kaluk** failures:

* `kaluk+A+Sg+Nom` does not generate!
* `kaluk` has no analyses either

**kukyirrek** failures:

* `kukyirrek+A+Sg+Nom` does not generate!
* `kukyirrek` has no analyses either

**werrk** failures:

* `werrk+A+Sg+Nom` does not generate!
* `werrk` has no analyses either

**weleberrk** failures:

* `weleberrk+A+Sg+Nom` does not generate!
* `weleberrk` has no analyses either

**djoleng** failures:

* `djoleng+A+Sg+Nom` does not generate!
* `djoleng` has no analyses either

**bokenh** failures:

* `bokenh+A+Sg+Nom` does not generate!
* `bokenh` has no analyses either

**kele** failures:

* `kele+A+Sg+Nom` does not generate!
* `kele` has no analyses either

**yerrikah** failures:

* `yerrikah+A+Sg+Nom` does not generate!
* `yerrikah` has no analyses either

**laddum** failures:

* `laddum+A+Sg+Nom` does not generate!
* `laddum` has no analyses either

**barnangarra** failures:

* `barnangarra+A+Sg+Nom` does not generate!
* `barnangarra` has no analyses either

**kanjdji** failures:

* `kanjdji+A+Sg+Nom` does not generate!
* `kanjdji` has no analyses either

**warnyak** failures:

* `warnyak+A+Sg+Nom` does not generate!
* `warnyak` has no analyses either

**mimbunje** failures:

* `mimbunje+A+Sg+Nom` does not generate!
* `mimbunje` has no analyses either

**kukyahwurd** failures:

* `kukyahwurd+A+Sg+Nom` does not generate!
* `kukyahwurd` has no analyses either

**dedjlahlarrk** failures:

* `dedjlahlarrk+A+Sg+Nom` does not generate!
* `dedjlahlarrk` has no analyses either

**walakkih** failures:

* `walakkih+A+Sg+Nom` does not generate!
* `walakkih` has no analyses either

**buldjdjarn** failures:

* `buldjdjarn+A+Sg+Nom` does not generate!
* `buldjdjarn` has no analyses either

**wern** failures:

* `wern+A+Sg+Nom` does not generate!
* `wern` has no analyses either

**burrk** failures:

* `burrk+A+Sg+Nom` does not generate!
* `burrk` has no analyses either

**marrmarr** failures:

* `marrmarr+A+Sg+Nom` does not generate!
* `marrmarr` has no analyses either

**djadmud** failures:

* `djadmud+A+Sg+Nom` does not generate!
* `djadmud` has no analyses either

**mandjad** failures:

* `mandjad+A+Sg+Nom` does not generate!
* `mandjad` has no analyses either

**mimbi** failures:

* `mimbi+A+Sg+Nom` does not generate!
* `mimbi` has no analyses either

**djarreh** failures:

* `djarreh+A+Sg+Nom` does not generate!
* `djarreh` has no analyses either

**buk** failures:

* `buk+A+Sg+Nom` does not generate!
* `buk` has no analyses either

**yahwurd** failures:

* `yahwurd+A+Sg+Nom` does not generate!
* `yahwurd` has no analyses either

**bang** failures:

* `bang+A+Sg+Nom` does not generate!
* `bang` has no analyses either

**kuyeng** failures:

* `kuyeng+A+Sg+Nom` does not generate!
* `kuyeng` has no analyses either

**bidmak** failures:

* `bidmak+A+Sg+Nom` does not generate!
* `bidmak` has no analyses either

**kudji** failures:

* `kudji+A+Sg+Nom` does not generate!
* `kudji` has no analyses either

**dangrayek** failures:

* `dangrayek+A+Sg+Nom` does not generate!
* `dangrayek` has no analyses either

**djokko** failures:

* `djokko+A+Sg+Nom` does not generate!
* `djokko` has no analyses either

**bonjdjek** failures:

* `bonjdjek+A+Sg+Nom` does not generate!
* `bonjdjek` has no analyses either

**kornamkuyeng** failures:

* `kornamkuyeng+A+Sg+Nom` does not generate!
* `kornamkuyeng` has no analyses either

**boyen** failures:

* `boyen+A+Sg+Nom` does not generate!
* `boyen` has no analyses either

**manmakmak** failures:

* `manmakmak+A+Sg+Nom` does not generate!
* `manmakmak` has no analyses either

**mulil** failures:

* `mulil+A+Sg+Nom` does not generate!
* `mulil` has no analyses either

**kunkarrngbakmeng** failures:

* `kunkarrngbakmeng+A+Sg+Nom` does not generate!
* `kunkarrngbakmeng` has no analyses either

**kih** failures:

* `kih+A+Sg+Nom` does not generate!
* `kih` has no analyses either

**kerrngekenh** failures:

* `kerrngekenh+A+Sg+Nom` does not generate!
* `kerrngekenh` has no analyses either

**malay** failures:

* `malay+A+Sg+Nom` does not generate!
* `malay` has no analyses either

**danjbik** failures:

* `danjbik+A+Sg+Nom` does not generate!
* `danjbik` has no analyses either

**mungu** failures:

* `mungu+A+Sg+Nom` does not generate!
* `mungu` has no analyses either

**rayek** failures:

* `rayek+A+Sg+Nom` does not generate!
* `rayek` has no analyses either

**mirndewern** failures:

* `mirndewern+A+Sg+Nom` does not generate!
* `mirndewern` has no analyses either

**nakuken** failures:

* `nakuken+A+Sg+Nom` does not generate!
* `nakuken` has no analyses either

**nud** failures:

* `nud+A+Sg+Nom` does not generate!
* `nud` has no analyses either

**bele** failures:

* `bele+A+Sg+Nom` does not generate!
* `bele` has no analyses either

**dedjdjumbung** failures:

* `dedjdjumbung+A+Sg+Nom` does not generate!
* `dedjdjumbung` has no analyses either

**djurdu** failures:

* `djurdu+A+Sg+Nom` does not generate!
* `djurdu` has no analyses either

**mankuk** failures:

* `mankuk+A+Sg+Nom` does not generate!
* `mankuk` has no analyses either

**murrngbang** failures:

* `murrngbang+A+Sg+Nom` does not generate!
* `murrngbang` has no analyses either

**bulkkidj** failures:

* `bulkkidj+A+Sg+Nom` does not generate!
* `bulkkidj` has no analyses either

**darleh** failures:

* `darleh+A+Sg+Nom` does not generate!
* `darleh` has no analyses either

**manjmak** failures:

* `manjmak+A+Sg+Nom` does not generate!
* `manjmak` has no analyses either

**kunbidkudji** failures:

* `kunbidkudji+A+Sg+Nom` does not generate!
* `kunbidkudji` has no analyses either

**babang** failures:

* `babang+A+Sg+Nom` does not generate!
* `babang` has no analyses either

**kangemak** failures:

* `kangemak+A+Sg+Nom` does not generate!
* `kangemak` has no analyses either

**balabbala** failures:

* `balabbala+A+Sg+Nom` does not generate!
* `balabbala` has no analyses either

**kuk** failures:

* `kuk+A+Sg+Nom` does not generate!
* `kuk` has no analyses either

## Lemma statistics
* 134 lemmas
* -100.0 % success

## Settings used

```json
{
    "adjectives": {
        "lemmatags": [
            "+A+Sg+Nom"
        ],
        "lexcfile": ".../adjectives.lexc"
    },
    "analyser": ".../analyser-gt-norm.hfstol",
    "generator": ".../generator-gt-norm.hfstol",
    "nouns": {
        "lemmatags": [
            "+N+Sg+Nom",
            "+N+Pl+Nom"
        ],
        "lexcfile": ".../nouns.lexc"
    },
    "propernouns": {
        "lemmatags": [
            "+N+Prop+Sg+Nom"
        ],
        "lexcfile": ".../propernouns.lexc"
    },
    "verbs": {
        "lemmatags": [
            "+V+Inf"
        ],
        "lexcfile": ".../verbs.lexc"
    }
}
```
